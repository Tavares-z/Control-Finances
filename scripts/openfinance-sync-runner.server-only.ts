// Stub de `server-only`/`client-only` para o runner rodar o módulo do app fora
// do bundler do Next (onde esses pacotes não resolvem / lançam). No-op de
// propósito — não altera comportamento do código de produção.
export {};
